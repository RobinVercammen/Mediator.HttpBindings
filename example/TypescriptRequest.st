import { IRequest } from '../irequest';
import { <first(first(class.Extends).GenericArguments)> } from './<first(first(class.Extends).GenericArguments)>';

export class <class.TypeName> extends IRequest\<<first(first(class.Extends).GenericArguments)>>{
  <class.Properties :{property | <property.Name>: <property.TypeName><if(property.ArrayLike)>[]<endif><\n>}>
}